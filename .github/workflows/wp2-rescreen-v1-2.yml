name: WP2 Rescreen Keyword V1.2

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/wp2-rescreen-v1-2.yml'

permissions:
  contents: write

jobs:
  rescreen:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        working-directory: My_work/03_data/pilot
        run: pip install -r requirements.txt
      - name: Run Pilot regression tests
        working-directory: My_work/03_data/pilot
        run: python -m unittest -v test_pilot_logic.py
      - name: Rescreen existing formal O/V corpus
        working-directory: My_work/03_data/pilot
        run: python screen_candidates.py output/github_candidates.jsonl output/vdp_at_2026-06-30.jsonl
      - name: Summarize v1.2 screening
        working-directory: My_work/03_data/pilot
        run: |
          python summarize_pilot.py --input output/pilot_screened.jsonl --out output/pilot_summary.json
          cp output/pilot_summary.json analysis/pilot_summary_keyword_v1_2.json
      - name: Rebuild 300-record review queue
        working-directory: My_work/03_data/pilot
        run: python build_review_queue.py --input output/pilot_screened.jsonl --n 300
      - name: Compare v1.1 and v1.2 summaries
        working-directory: My_work/03_data/pilot
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          a = json.loads(Path('analysis/pilot_summary_keyword_v1_1.json').read_text())
          b = json.loads(Path('analysis/pilot_summary_keyword_v1_2.json').read_text())
          def diff_map(key):
              aa, bb = a.get(key, {}), b.get(key, {})
              keys = sorted(set(aa) | set(bb))
              return {k: {'v1_1': aa.get(k,0), 'v1_2': bb.get(k,0), 'delta': bb.get(k,0)-aa.get(k,0)} for k in keys}
          report = {
              'n_v1_1': a.get('n'), 'n_v1_2': b.get('n'),
              'scope': diff_map('by_scope'),
              'confidence': diff_map('by_confidence'),
              'false_positive': diff_map('by_false_positive'),
              'primary_family': diff_map('by_primary_family'),
              'interpretation_guardrail': 'This is a screening-regression comparison, not accuracy evidence. Human gold review is still required.'
          }
          Path('analysis/screening_regression_v1_1_to_v1_2.json').write_text(json.dumps(report, indent=2), encoding='utf-8')
          PY
      - name: Write rescreen metadata
        working-directory: My_work/03_data/pilot
        run: |
          python - <<'PY'
          import json, datetime
          from pathlib import Path
          meta = {
            'run_at_utc': datetime.datetime.now(datetime.timezone.utc).isoformat(),
            'mode': 'RESCREEN_EXISTING_FORMAL_CORPUS',
            'taxonomy_version': '1.1',
            'keyword_version': '1.2',
            'input_sources': ['output/github_candidates.jsonl', 'output/vdp_at_2026-06-30.jsonl'],
            'external_api_calls': False,
            'note': 'Regression-only rescreen after XAI/AI phrase recall expansion and GitHub hygiene exclusions.'
          }
          Path('analysis/rescreen_v1_2_metadata.json').write_text(json.dumps(meta, indent=2), encoding='utf-8')
          PY
      - name: Commit rescreen outputs
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add My_work/03_data/pilot/output/pilot_screened.jsonl \
                  My_work/03_data/pilot/output/pilot_summary.json \
                  My_work/03_data/pilot/output/manual_review_queue_compact.jsonl \
                  My_work/03_data/pilot/output/manual_review_queue_compact.csv \
                  My_work/03_data/pilot/analysis
          if git diff --cached --quiet; then
            echo 'No rescreen changes.'
          else
            git commit -m 'WP2: rescreen formal Pilot with keyword v1.2'
            git fetch origin main
            git rebase origin/main
            git push origin HEAD:main
          fi
