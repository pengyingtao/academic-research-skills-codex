name: WP2 Append Execution Log (Deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow notice
        run: |
          echo 'This append workflow is intentionally disabled.'
          echo 'Its historical Patent public-S3 assumption was superseded by later HTTP 403 execution evidence.'
          echo 'Use My_work/执行记录/执行日志.md and newer date-scoped log workflows as the authoritative record.'
